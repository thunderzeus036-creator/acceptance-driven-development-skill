# Project Index

> Auto-generated by Dataview. No manual maintenance needed.

---

## All Projects

```dataview
TABLE date, status, tags
FROM "projects"
WHERE contains(tags, "project")
SORT date DESC
```

---

## By Tech Stack

### C++ / Qt

```dataview
TABLE status
FROM "projects"
WHERE contains(tags, "Qt") AND contains(tags, "project")
SORT file.name ASC
```

---

## By Status

### Active / Maintained

```dataview
TABLE tags
FROM "projects"
WHERE contains(status, "maintained") AND contains(tags, "project")
```

---

## Recently Modified

```dataview
TABLE file.mtime as "Last Modified"
FROM "projects"
WHERE contains(tags, "project")
SORT file.mtime DESC
LIMIT 10
```
